# 源代码鉴别材料（后30页）

软件名称：AI语文助手 - 体育生语文基础智能辅导系统
软件版本：V3.0.0

---

## 第 1 页（共1500行中的第1451-1500行）

**文件名：src/pages/Chat.jsx**

```javascript
import { useState, useEffect, useRef } from 'react'
import { Card, Input, Button, List, Typography, Spin, message } from 'antd'
import { SendOutlined, LoadingOutlined, UserOutlined, RobotOutlined, ClearOutlined, CloseOutlined, CheckCircleOutlined } from '@ant-design/icons'

const { TextArea } = Input
const { Title, Text, Paragraph } = Typography

import { aiChatService } from '../services/aiChatService'
import { studyRecordService } from '../services/studyRecordService'

const Chat = () => {
  const [messages, setMessages] = useState([])
  const [inputValue, setInputValue] = useState('')
  const [isLoading, setIsLoading] = useState(false)
  const [chatHistory, setChatHistory] = useState([])
  const [currentChatId, setCurrentChatId] = useState(null)
  const messagesEndRef = useRef(null)
  const typewriterRef = useRef(null)

  // 从本地存储加载聊天历史
  useEffect(() => {
    loadChatHistory()
  }, [])

  // 自动滚动到最新消息
  useEffect(() => {
    scrollToBottom()
  }, [messages])

  // 组件卸载时清除定时器
  useEffect(() => {
    return () => {
      if (typewriterRef.current) {
        clearInterval(typewriterRef.current)
      }
    }
  }, [])

  const loadChatHistory = () => {
    const saved = localStorage.getItem('chatHistory')
    if (saved) {
      try {
        const history = JSON.parse(saved)
        setChatHistory(history)
        // 如果有历史记录，加载最新的一次聊天
        if (history.length > 0) {
          const latestChat = history[history.length - 1]
          setCurrentChatId(latestChat.id)
          setMessages(latestChat.messages)
        }
      } catch (error) {
        console.error('加载聊天历史失败:', error)
        message.error('加载聊天历史失败')
      }
    }
  }

  const saveChatHistory = () => {
    if (messages.length === 0) return

    // 检查是否已存在相同ID的聊天
    const existingIndex = chatHistory.findIndex(chat => chat.id === currentChatId)
    const updatedChat = {
      id: currentChatId || Date.now(),
      title: messages[0].content.substring(0, 30) + (messages[0].content.length > 30 ? '...' : ''),
      messages: messages,
      date: new Date().toLocaleString('zh-CN')
    }

    let newHistory
    if (existingIndex >= 0) {
      newHistory = [...chatHistory]
      newHistory[existingIndex] = updatedChat
    } else {
      newHistory = [updatedChat, ...chatHistory.slice(0, 9)] // 只保留最近10条聊天记录
      setCurrentChatId(updatedChat.id)
    }

    setChatHistory(newHistory)
    localStorage.setItem('chatHistory', JSON.stringify(newHistory))
  }

  const scrollToBottom = () => {
    messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' })
  }

  // 打字机效果函数
  const typewriterEffect = (fullText, messageId) => {
    let currentIndex = 0
    const delay = 30 // 每个字符的延迟时间（毫秒）

    // 清除之前的定时器
    if (typewriterRef.current) {
      clearInterval(typewriterRef.current)
    }

    // 创建定时器逐字显示文本
    typewriterRef.current = setInterval(() => {
      if (currentIndex <= fullText.length) {
        const partialText = fullText.substring(0, currentIndex)

        setMessages(prevMessages =>
          prevMessages.map(msg =>
            msg.id === messageId
              ? { ...msg, content: partialText, isComplete: currentIndex === fullText.length }
              : msg
          )
        )

        currentIndex++
      } else {
        // 文本显示完成，清除定时器
        clearInterval(typewriterRef.current)
        typewriterRef.current = null
        saveChatHistory()
      }
    }, delay)
  }

  // 发送消息
  const sendMessage = async () => {
    if (!inputValue.trim() || isLoading) return

    const userMessage = {
      id: Date.now(),
      type: 'user',
      content: inputValue.trim(),
      timestamp: new Date().toLocaleTimeString()
    }

    setMessages([...messages, userMessage])
    setInputValue('')
    setIsLoading(true)

    try {
      // 创建一个加载中的机器人消息
      const loadingBotMessageId = Date.now() + 1
      const loadingBotMessage = {
        id: loadingBotMessageId,
        type: 'bot',
        content: '',
        timestamp: new Date().toLocaleTimeString(),
        isComplete: false
      }

      setMessages(prev => [...prev, loadingBotMessage])

      // 调用DeepSeek API
      const response = await callDeepSeekAPI(userMessage.content)

      // 开始打字机效果
      typewriterEffect(response, loadingBotMessageId)
    } catch (error) {
      // 添加错误消息
      const errorMessage = {
        id: Date.now() + 1,
        type: 'bot',
        content: '很抱歉，获取响应失败，请稍后重试。',
        timestamp: new Date().toLocaleTimeString(),
        isComplete: true
      }
      setMessages(prev => [...prev, errorMessage])
      message.error('获取响应失败，请稍后重试')
      console.error('API调用失败:', error)
    } finally {
      setIsLoading(false)
    }
  }

  // 调用DeepSeek API
  const callDeepSeekAPI = async (prompt) => {
    try {
      // 构建消息
      const messages = [
        {
          role: 'system',
          content: '你是一个专业的语文学习顾问，擅长回答各种语文相关问题，并能提供个性化的学习建议。'
        },
        {
          role: 'user',
          content: prompt
        }
      ]

      // 调用API
      const response = await aiChatService.callDeepSeekAPI(messages)
      return response
    } catch (error) {
      console.error('DeepSeek API调用失败:', error)
      throw error
    }
  }

  // 讨论学习情况
  const discussStudyProgress = async () => {
    setIsLoading(true)

    try {
      // 获取学习数据
      const statistics = await studyRecordService.getStudyStatistics()
      const categoryData = await studyRecordService.getCategoryStudyData()
      const recentRecords = await studyRecordService.getStudyRecords({ pageSize: 5 })

      // 构建学习数据对象
      const studyData = {
        statistics,
        categoryData,
        recentRecords: recentRecords.data
      }

      // 创建用户消息
      const userMessage = {
        id: Date.now(),
        type: 'user',
        content: '请分析我的学习情况并提供建议',
        timestamp: new Date().toLocaleTimeString()
      }

      setMessages(prev => [...prev, userMessage])

      // 创建一个加载中的机器人消息
      const loadingBotMessageId = Date.now() + 1
      const loadingBotMessage = {
        id: loadingBotMessageId,
        type: 'bot',
        content: '',
        timestamp: new Date().toLocaleTimeString(),
        isComplete: false
      }

      setMessages(prev => [...prev, loadingBotMessage])

      // 调用AI服务分析学习情况
      const response = await aiChatService.discussStudyProgress(studyData)

      // 开始打字机效果
      typewriterEffect(response, loadingBotMessageId)
    } catch (error) {
      // 添加错误消息
      const errorMessage = {
        id: Date.now() + 1,
        type: 'bot',
        content: '很抱歉，获取学习分析失败，请稍后重试。',
        timestamp: new Date().toLocaleTimeString(),
        isComplete: true
      }
      setMessages(prev => [...prev, errorMessage])
      message.error('获取学习分析失败，请稍后重试')
      console.error('学习分析失败:', error)
    } finally {
      setIsLoading(false)
    }
  }

  // 新建聊天
  const startNewChat = () => {
    setMessages([])
    setCurrentChatId(null)
  }

  // 加载历史聊天
  const loadChat = (chat) => {
    setCurrentChatId(chat.id)
    setMessages(chat.messages)
  }

  // 删除历史聊天
  const deleteChat = (chatId, e) => {
    e.stopPropagation()
    const newHistory = chatHistory.filter(chat => chat.id !== chatId)
    setChatHistory(newHistory)
    localStorage.setItem('chatHistory', JSON.stringify(newHistory))

    // 如果删除的是当前聊天，新建一个聊天
    if (chatId === currentChatId) {
      startNewChat()
    }
  }

  // 清空当前聊天
  const clearCurrentChat = () => {
    setMessages([])
  }

  return (
    <div className="min-h-screen bg-gray-50 py-8 px-4">
      <div className="max-w-5xl mx-auto">
        <div className="flex items-center justify-between mb-6">
            <div className="flex items-center">
              <RobotOutlined className="text-blue-600 mr-2" />
              <Title level={2}>智能语文助手</Title>
            </div>
            <div className="flex space-x-2">
              <Button onClick={discussStudyProgress} type="default" icon={<CheckCircleOutlined />}>学习分析</Button>
              <Button onClick={clearCurrentChat} icon={<ClearOutlined />}>清空聊天</Button>
              <Button type="primary" onClick={startNewChat}>新建聊天</Button>
            </div>
          </div>

        <div className="flex flex-col md:flex-row gap-6">
          {/* 聊天历史侧边栏 */}
          <Card className="md:w-1/4 h-[500px] overflow-y-auto">
            <Title level={5} className="mb-4">聊天历史</Title>
            {chatHistory.length === 0 ? (
              <Text type="secondary">暂无聊天记录</Text>
            ) : (
              <List
                dataSource={chatHistory}
                renderItem={(chat) => (
                  <List.Item
                    className={`cursor-pointer p-2 rounded mb-2 hover:bg-gray-100 ${currentChatId === chat.id ? 'bg-blue-50' : ''}`}
                    onClick={() => loadChat(chat)}
                  >
                    <div className="w-full relative">
                      <div className="font-medium line-clamp-1">{chat.title}</div>
                      <Text type="secondary" className="text-xs">{chat.date}</Text>
                      <Button
                        type="text"
                        size="small"
                        icon={<CloseOutlined />}
                        className="absolute right-0 top-0 text-gray-400 hover:text-red-500"
                        onClick={(e) => deleteChat(chat.id, e)}
                      />
                    </div>
                  </List.Item>
                )}
              />
            )}
          </Card>

          {/* 聊天主界面 */}
          <Card className="md:w-3/4 flex flex-col h-[500px]">
            {/* 聊天内容区域 */}
            <div className="flex-1 overflow-y-auto p-4">
              {messages.length === 0 ? (
                <div className="flex flex-col items-center justify-center h-full text-gray-400">
                  <RobotOutlined className="text-4xl mb-4" />
                  <Text>你好！我是你的智能语文助手，有什么问题可以问我哦~</Text>
                </div>
              ) : (
                <List
                  dataSource={messages}
                  renderItem={(message) => (
                    <List.Item className={`${message.type === 'user' ? 'justify-end' : 'justify-start'} mb-4`}>
                      <div className={`flex items-start max-w-[80%] ${message.type === 'user' ? 'flex-row-reverse' : ''}`}>
                        {message.type === 'user' ? (
                          <UserOutlined className="text-blue-500 mr-2 mt-1" />
                        ) : (
                          <RobotOutlined className="text-green-500 mr-2 mt-1" />
                        )}
                        <div className={`${message.type === 'user' ? 'bg-blue-500 text-white rounded-tr-none' : 'bg-gray-200 rounded-tl-none'} px-4 py-2 rounded-lg`}>
                          <Paragraph className="m-0 whitespace-pre-wrap">{message.content}</Paragraph>
                          <Text className="block text-xs opacity-70 mt-1">{message.timestamp}</Text>
                        </div>
                      </div>
                    </List.Item>
                  )}
                />
              )}
              <div ref={messagesEndRef} />
            </div>

            {/* 输入区域 */}
            <div className="border-t p-4">
              <div className="flex gap-2">
                <TextArea
                  value={inputValue}
                  onChange={(e) => setInputValue(e.target.value)}
                  placeholder="请输入您的问题..."
                  rows={3}
                  onPressEnter={(e) => {
                    if (e.ctrlKey || e.metaKey) {
                      sendMessage()
                    }
                  }}
                />
                <Button
                  type="primary"
                  icon={isLoading ? <LoadingOutlined /> : <SendOutlined />}
                  onClick={sendMessage}
                  disabled={!inputValue.trim() || isLoading}
                  className="self-end"
                />
              </div>
              <Text type="secondary" className="text-xs mt-2 block">
                {isLoading ? '正在思考...' : '按 Ctrl+Enter 或点击发送按钮'}
              </Text>
            </div>
          </Card>
        </div>

        {/* 功能说明卡片 */}
        <Card title="功能说明" className="mt-6">
          <List
            dataSource={[
              '支持错别字纠错、古诗词赏析、汉字拼音、成语学习等语文相关问题',
              '聊天记录会保存在本地，最多保存10条历史记录',
              '您可以随时新建聊天或删除历史记录',
              '输入框支持多行文本，按 Ctrl+Enter 快速发送消息',
              '点击"学习分析"按钮获取基于您学习数据的个性化建议'
            ]}
            renderItem={(item, index) => (
              <List.Item>
                <CheckCircleOutlined className="text-green-500 mr-2" />
                <Text>{item}</Text>
              </List.Item>
            )}
          />
        </Card>
      </div>
    </div>
  )
}

export default Chat
```

---

## 第 2 页

**文件名：src/pages/Vocabulary.jsx（节选结尾部分）**

```javascript
  return (
    <div className="vocabulary-container">
      <Card className="search-card">
        <div className="search-input-wrapper">
          <Input
            placeholder="请输入要查询的词语"
            value={searchWord}
            onChange={(e) => setSearchWord(e.target.value)}
            onPressEnter={handleSearch}
            className="search-input"
            prefix={<SearchOutlined />}
          />
          <Button type="primary" onClick={handleSearch} className="search-btn">
            查询
          </Button>
        </div>
      </Card>

      {loading ? (
        <div className="loading-wrapper">
          <Spin size="large" tip="查询中..." />
        </div>
      ) : (
        <Tabs activeKey={currentTab} onChange={setCurrentTab} className="vocabulary-tabs">
          <TabPane tab="词语详情" key="1">
            {wordDetail ? (
              <div className="word-detail">
                <div className="word-header">
                  <h2>{wordDetail.word}</h2>
                  <span className="pinyin">{wordDetail.pinyin}</span>
                  <Tooltip title="播放发音">
                    <Button
                      type="text"
                      icon={<SoundOutlined />}
                      onClick={playPronunciation}
                      className="pronunciation-btn"
                    />
                  </Tooltip>
                  <Tooltip title={isWordFavorite(wordDetail.word) ? '取消收藏' : '收藏'}>
                    <Button
                      type="text"
                      icon={isWordFavorite(wordDetail.word) ? <StarFilled /> : <StarOutlined />}
                      onClick={() => toggleFavorite(wordDetail)}
                      className="favorite-btn"
                      style={{ color: isWordFavorite(wordDetail.word) ? '#ff4d4f' : '' }}
                    />
                  </Tooltip>
                </div>

                <Divider />

                <div className="word-info">
                  {wordDetail.partOfSpeech && (
                    <div className="info-item">
                      <Tag color="blue">{wordDetail.partOfSpeech}</Tag>
                    </div>
                  )}

                  {wordDetail.radical && (
                    <div className="info-item">
                      <span className="info-label">部首：</span>
                      <span className="info-value">{wordDetail.radical}</span>
                    </div>
                  )}

                  {wordDetail.strokeCount && (
                    <div className="info-item">
                      <span className="info-label">笔画：</span>
                      <span className="info-value">{wordDetail.strokeCount}</span>
                    </div>
                  )}
                </div>

                <Divider />

                <div className="definitions">
                  <h3>释义</h3>
                  <List
                    dataSource={wordDetail.definitions}
                    renderItem={(item, index) => (
                      <List.Item className="definition-item">
                        <span className="definition-number">{index + 1}.</span>
                        <span className="definition-text">{item}</span>
                      </List.Item>
                    )}
                  />
                </div>

                {wordDetail.examples && wordDetail.examples.length > 0 && (
                  <div className="examples">
                    <h3>例句</h3>
                    <List
                      dataSource={wordDetail.examples}
                      renderItem={(item, index) => (
                        <List.Item className="example-item">
                          <span className="example-text">{item}</span>
                        </List.Item>
                      )}
                    />
                  </div>
                )}

                {wordDetail.collocations && wordDetail.collocations.length > 0 && (
                  <div className="collocations">
                    <h3>常用搭配</h3>
                    <div className="collocation-tags">
                      {wordDetail.collocations.map((collocation, index) => (
                        <Tag key={index} color="purple">{collocation}</Tag>
                      ))}
                    </div>
                  </div>
                )}

                {wordDetail.usageNotes && (
                  <div className="usage-notes">
                    <h3>用法说明</h3>
                    <p>{wordDetail.usageNotes}</p>
                  </div>
                )}

                {wordDetail.origin && (
                  <div className="origin">
                    <h3>出处</h3>
                    <p>{wordDetail.origin}</p>
                  </div>
                )}
              </div>
            ) : (
              <Empty description="请输入词语进行查询" />
            )}
          </TabPane>

          <TabPane tab="相关词语" key="2">
            {relatedWords.synonyms.length > 0 && (
              <div className="related-words-section">
                <h3>近义词</h3>
                <div className="related-words-tags">
                  {relatedWords.synonyms.map((word, index) => (
                    <Tag key={index} color="green" className="related-word-tag">{word}</Tag>
                  ))}
                </div>
              </div>
            )}

            {relatedWords.antonyms.length > 0 && (
              <div className="related-words-section">
                <h3>反义词</h3>
                <div className="related-words-tags">
                  {relatedWords.antonyms.map((word, index) => (
                    <Tag key={index} color="red" className="related-word-tag">{word}</Tag>
                  ))}
                </div>
              </div>
            )}

            {relatedWords.synonyms.length === 0 &&
             relatedWords.antonyms.length === 0 &&
             relatedWords.phrases.length === 0 &&
             relatedWords.examples.length === 0 && (
              <Empty description="暂无相关词语" />
            )}
          </TabPane>

          <TabPane tab="我的收藏" key="3">
            <div className="favorite-section">
              {favoriteWords.length > 0 ? (
                <List
                  dataSource={favoriteWords}
                  renderItem={(word) => (
                    <List.Item
                      className="favorite-item"
                      actions={[
                        <Button type="text" icon={<StarFilled />} onClick={() => toggleFavorite(word)} style={{ color: '#ff4d4f' }}>
                          取消收藏
                        </Button>
                      ]}
                    >
                      <List.Item.Meta
                        avatar={<Avatar>{word.word.substring(0, 1)}</Avatar>}
                        title={word.word}
                        description={word.pinyin}
                      />
                    </List.Item>
                  )}
                />
              ) : (
                <Empty description="暂无收藏的词语" />
              )}
            </div>
          </TabPane>
        </Tabs>
      )}
    </div>
  )
}

export default Vocabulary
```

---

## 第 3 页

**文件名：src/pages/Exercise.jsx（节选结尾部分）**

```javascript
  return (
    <div className="exercise-container">
      {renderMainContent()}
    </div>
  )
}

export default Exercise
```

**文件名：src/pages/StudyRecord.jsx**

```javascript
import React, { useState, useEffect } from 'react'
import { Card, Table, DatePicker, Select, Button, Space, Tag, message } from 'antd'
import { DownloadOutlined, EyeOutlined, FilterOutlined } from '@ant-design/icons'
import dayjs from 'dayjs'
import { studyRecordService } from '../services/studyRecordService'

const { RangePicker } = DatePicker
const { Option } = Select

const StudyRecord = () => {
  const [records, setRecords] = useState([])
  const [loading, setLoading] = useState(false)
  const [pagination, setPagination] = useState({
    current: 1,
    pageSize: 20,
    total: 0
  })
  const [filters, setFilters] = useState({
    type: 'all',
    dateRange: null
  })

  // 类型选项
  const typeOptions = [
    { label: '全部', value: 'all' },
    { label: '词语学习', value: 'vocabulary' },
    { label: '古诗词学习', value: 'literature' },
    { label: '熟语习语', value: 'idiom' },
    { label: '练习', value: 'exercise' },
    { label: '拼音学习', value: 'pinyin' },
    { label: '文本纠错', value: 'correction' }
  ]

  // 加载学习记录
  useEffect(() => {
    loadRecords()
  }, [pagination.current, pagination.pageSize, filters])

  const loadRecords = async () => {
    setLoading(true)
    try {
      const params = {
        page: pagination.current,
        pageSize: pagination.pageSize,
        type: filters.type
      }

      if (filters.dateRange && filters.dateRange.length === 2) {
        params.startDate = filters.dateRange[0].format('YYYY-MM-DD')
        params.endDate = filters.dateRange[1].format('YYYY-MM-DD')
      }

      const result = await studyRecordService.getStudyRecords(params)

      setRecords(result.data)
      setPagination(prev => ({
        ...prev,
        total: result.total
      }))
    } catch (error) {
      message.error('加载学习记录失败')
      console.error('加载学习记录失败:', error)
    } finally {
      setLoading(false)
    }
  }

  // 处理表格变化
  const handleTableChange = (newPagination) => {
    setPagination(newPagination)
  }

  // 处理类型筛选
  const handleTypeChange = (value) => {
    setFilters(prev => ({ ...prev, type: value }))
    setPagination(prev => ({ ...prev, current: 1 }))
  }

  // 处理日期范围变化
  const handleDateRangeChange = (dates) => {
    setFilters(prev => ({ ...prev, dateRange: dates }))
    setPagination(prev => ({ ...prev, current: 1 }))
  }

  // 重置筛选
  const handleResetFilters = () => {
    setFilters({
      type: 'all',
      dateRange: null
    })
    setPagination(prev => ({ ...prev, current: 1 }))
  }

  // 导出记录
  const handleExport = () => {
    // 导出逻辑
    message.info('导出功能开发中')
  }

  // 获取类型标签颜色
  const getTypeColor = (type) => {
    const colorMap = {
      vocabulary: 'blue',
      literature: 'green',
      idiom: 'orange',
      exercise: 'purple',
      pinyin: 'cyan',
      correction: 'red'
    }
    return colorMap[type] || 'default'
  }

  // 获取类型标签文本
  const getTypeText = (type) => {
    const textMap = {
      vocabulary: '词语学习',
      literature: '古诗词',
      idiom: '熟语习语',
      exercise: '练习',
      pinyin: '拼音学习',
      correction: '文本纠错'
    }
    return textMap[type] || type
  }

  // 表格列定义
  const columns = [
    {
      title: '序号',
      key: 'index',
      width: 60,
      render: (_, __, index) => (pagination.current - 1) * pagination.pageSize + index + 1
    },
    {
      title: '学习类型',
      dataIndex: 'type',
      key: 'type',
      width: 120,
      render: (type) => (
        <Tag color={getTypeColor(type)}>{getTypeText(type)}</Tag>
      )
    },
    {
      title: '学习内容',
      dataIndex: 'title',
      key: 'title',
      ellipsis: true
    },
    {
      title: '学习日期',
      dataIndex: 'studyDate',
      key: 'studyDate',
      width: 120
    },
    {
      title: '时长',
      dataIndex: 'durationText',
      key: 'duration',
      width: 100
    },
    {
      title: '得分',
      dataIndex: 'score',
      key: 'score',
      width: 80,
      render: (score) => score !== null ? score : '-'
    },
    {
      title: '正确率',
      dataIndex: 'completionRate',
      key: 'completionRate',
      width: 100,
      render: (rate) => `${rate}%`
    }
  ]

  return (
    <div className="study-record-container">
      <Card title="学习记录">
        {/* 筛选区域 */}
        <Space style={{ marginBottom: 16 }} size="middle">
          <Select
            style={{ width: 150 }}
            value={filters.type}
            onChange={handleTypeChange}
            placeholder="选择类型"
          >
            {typeOptions.map(option => (
              <Option key={option.value} value={option.value}>
                {option.label}
              </Option>
            ))}
          </Select>

          <RangePicker
            value={filters.dateRange}
            onChange={handleDateRangeChange}
            placeholder={['开始日期', '结束日期']}
          />

          <Button icon={<FilterOutlined />} onClick={handleResetFilters}>
            重置
          </Button>

          <Button icon={<DownloadOutlined />} onClick={handleExport}>
            导出
          </Button>
        </Space>

        {/* 表格 */}
        <Table
          columns={columns}
          dataSource={records}
          loading={loading}
          pagination={pagination}
          onChange={handleTableChange}
          rowKey="id"
        />
      </Card>
    </div>
  )
}

export default StudyRecord
```

---

## 第 4 页

**文件名：src/pages/StudyReport.jsx**

```javascript
import React, { useState, useEffect } from 'react'
import { Card, Row, Col, DatePicker, Select, Spin, Empty } from 'antd'
import { Line, Column, Pie } from '@ant-design/plots'
import { ReloadOutlined, DownloadOutlined } from '@ant-design/icons'
import { Button, message } from 'antd'
import dayjs from 'dayjs'
import { studyRecordService } from '../services/studyRecordService'

const { RangePicker } = DatePicker

const StudyReport = () => {
  const [loading, setLoading] = useState(false)
  const [dateRange, setDateRange] = useState([dayjs().subtract(30, 'day'), dayjs()])
  const [statistics, setStatistics] = useState(null)

  useEffect(() => {
    loadStatistics()
  }, [dateRange])

  const loadStatistics = async () => {
    setLoading(true)
    try {
      const days = dateRange[1].diff(dateRange[0], 'day') + 1
      const stats = await studyRecordService.getStudyStatistics(days)
      setStatistics(stats)
    } catch (error) {
      message.error('加载统计数据失败')
      console.error('加载统计数据失败:', error)
    } finally {
      setLoading(false)
    }
  }

  const handleRefresh = () => {
    loadStatistics()
    message.success('数据已刷新')
  }

  const handleExport = () => {
    message.info('导出功能开发中')
  }

  // 准备学习时长趋势数据
  const getTrendData = () => {
    if (!statistics || !statistics.categoryData) return []

    return statistics.categoryData.map(item => ({
      date: item.category,
      value: item.studyTime
    }))
  }

  // 准备分类占比数据
  const getCategoryData = () => {
    if (!statistics || !statistics.categoryData) return []

    return statistics.categoryData.map(item => ({
      type: item.category,
      value: item.studyTime
    }))
  }

  // 准备练习统计图表数据
  const getExerciseData = () => {
    if (!statistics || !statistics.exerciseStats) return []

    return [
      {
        type: '总练习数',
        value: statistics.exerciseStats.totalExercises || 0
      },
      {
        type: '平均分',
        value: statistics.exerciseStats.averageScore || 0
      },
      {
        type: '总题数',
        value: statistics.exerciseStats.totalQuestions || 0
      },
      {
        type: '正确题数',
        value: statistics.exerciseStats.totalCorrect || 0
      }
    ]
  }

  const trendConfig = {
    data: getTrendData(),
    xField: 'date',
    yField: 'value',
    smooth: true,
    point: {
      size: 5,
      shape: 'diamond'
    },
    label: {
      style: {
        fill: '#aaa'
      }
    },
    tooltip: {
      formatter: (datum) => {
        return { name: '学习时长', value: datum.value + ' 分钟' }
      }
    }
  }

  const categoryConfig = {
    data: getCategoryData(),
    angleField: 'value',
    colorField: 'type',
    radius: 0.8,
    innerRadius: 0.5,
    label: {
      type: 'inner',
      offset: '-50%',
      content: '{value}',
      style: {
        textAlign: 'center',
        fontSize: 14,
        fill: '#fff'
      }
    },
    tooltip: {
      formatter: (datum) => {
        return { name: datum.type, value: datum.value + ' 分钟' }
      }
    }
  }

  const exerciseConfig = {
    data: getExerciseData(),
    xField: 'type',
    yField: 'value',
    label: {
      position: 'top',
      style: {
        fill: '#000'
      }
    },
    tooltip: {
      formatter: (datum) => {
        return { name: datum.type, value: datum.value }
      }
    }
  }

  return (
    <div className="study-report-container">
      <Card
        title="学习报告"
        extra={
          <Button icon={<ReloadOutlined />} onClick={handleRefresh}>
            刷新
          </Button>
        }
      >
        {/* 日期选择 */}
        <Row style={{ marginBottom: 24 }}>
          <Col span={12}>
            <RangePicker
              value={dateRange}
              onChange={setDateRange}
              style={{ marginRight: 16 }}
            />
            <Button icon={<DownloadOutlined />} onClick={handleExport}>
              导出报告
            </Button>
          </Col>
        </Row>

        {loading ? (
          <div style={{ textAlign: 'center', padding: '100px 0' }}>
            <Spin size="large" tip="加载中..." />
          </div>
        ) : statistics && statistics.categoryData && statistics.categoryData.length > 0 ? (
          <Row gutter={[16, 16]}>
            {/* 学习时长趋势 */}
            <Col xs={24} md={12}>
              <Card title="学习时长趋势" bordered={false}>
                <Line {...trendConfig} height={300} />
              </Card>
            </Col>

            {/* 分类占比 */}
            <Col xs={24} md={12}>
              <Card title="学习分类占比" bordered={false}>
                <Pie {...categoryConfig} height={300} />
              </Card>
            </Col>

            {/* 练习统计 */}
            <Col xs={24}>
              <Card title="练习统计" bordered={false}>
                <Column {...exerciseConfig} height={250} />
              </Card>
            </Col>

            {/* 数据概览 */}
            <Col xs={24}>
              <Card title="数据概览" bordered={false}>
                <Row gutter={16}>
                  <Col span={6}>
                    <div style={{ textAlign: 'center' }}>
                      <div style={{ fontSize: 32, color: '#1890ff', fontWeight: 'bold' }}>
                        {statistics.totalStudyTime}
                      </div>
                      <div style={{ color: '#666' }}>总学习时长(分钟)</div>
                    </div>
                  </Col>
                  <Col span={6}>
                    <div style={{ textAlign: 'center' }}>
                      <div style={{ fontSize: 32, color: '#52c41a', fontWeight: 'bold' }}>
                        {statistics.studyDays}
                      </div>
                      <div style={{ color: '#666' }}>学习天数</div>
                    </div>
                  </Col>
                  <Col span={6}>
                    <div style={{ textAlign: 'center' }}>
                      <div style={{ fontSize: 32, color: '#faad14', fontWeight: 'bold' }}>
                        {statistics.averageDailyTime}
                      </div>
                      <div style={{ color: '#666' }}>日均学习(分钟)</div>
                    </div>
                  </Col>
                  <Col span={6}>
                    <div style={{ textAlign: 'center' }}>
                      <div style={{ fontSize: 32, color: '#f5222d', fontWeight: 'bold' }}>
                        {statistics.exerciseStats?.accuracy || 0}%
                      </div>
                      <div style={{ color: '#666' }}>正确率</div>
                    </div>
                  </Col>
                </Row>
              </Card>
            </Col>
          </Row>
        ) : (
          <Empty description="暂无数据" />
        )}
      </Card>
    </div>
  )
}

export default StudyReport
```

---

## 第 5 页

**文件名：src/pages/StudyAnalysis.jsx**

```javascript
import React, { useState, useEffect } from 'react'
import { Card, Row, Col, Spin, Empty, Progress, Tag } from 'antd'
import { Radar } from '@ant-design/plots'
import { ReloadOutlined } from '@ant-design/icons'
import { Button, message, List } from 'antd'
import { studyRecordService } from '../services/studyRecordService'

const StudyAnalysis = () => {
  const [loading, setLoading] = useState(false)
  const [progressList, setProgressList] = useState([])
  const [recommendations, setRecommendations] = useState([])

  useEffect(() => {
    loadAnalysisData()
  }, [])

  const loadAnalysisData = async () => {
    setLoading(true)
    try {
      const progress = await studyRecordService.getProgressByType()
      setProgressList(progress)

      // 根据进度生成建议
      generateRecommendations(progress)
    } catch (error) {
      message.error('加载分析数据失败')
      console.error('加载分析数据失败:', error)
    } finally {
      setLoading(false)
    }
  }

  const generateRecommendations = (progress) => {
    const recs = []

    progress.forEach(item => {
      if (item.progress < 30) {
        recs.push({
          type: item.type,
          priority: 'high',
          message: `${item.name}掌握程度较低，建议加强练习`
        })
      } else if (item.progress < 60) {
        recs.push({
          type: item.type,
          priority: 'medium',
          message: `${item.name}有待提高，继续加油`
        })
      }
    })

    setRecommendations(recs)
  }

  const handleRefresh = () => {
    loadAnalysisData()
    message.success('数据已刷新')
  }

  // 准备雷达图数据
  const getRadarData = () => {
    return progressList.map(item => ({
      subject: item.name,
      value: item.progress,
      fullMark: 100
    }))
  }

  const radarConfig = {
    data: getRadarData(),
    xField: 'subject',
    yField: 'value',
    area: {
      style: {
        fillOpacity: 0.2,
        fill: '#1890ff'
      }
    },
    point: {
      size: 4
    },
    tooltip: {
      formatter: (datum) => {
        return { name: datum.subject, value: datum.value + '%' }
      }
    }
  }

  const getProgressColor = (progress) => {
    if (progress >= 80) return '#52c41a'
    if (progress >= 60) return '#faad14'
    return '#f5222d'
  }

  const getPriorityColor = (priority) => {
    if (priority === 'high') return 'red'
    return 'orange'
  }

  const getPriorityText = (priority) => {
    if (priority === 'high') return '高'
    return '中'
  }

  return (
    <div className="study-analysis-container">
      <Card
        title="学习分析"
        extra={
          <Button icon={<ReloadOutlined />} onClick={handleRefresh}>
            刷新
          </Button>
        }
      >
        {loading ? (
          <div style={{ textAlign: 'center', padding: '100px 0' }}>
            <Spin size="large" tip="分析中..." />
          </div>
        ) : progressList.length > 0 ? (
          <Row gutter={[16, 16]}>
            {/* 能力雷达图 */}
            <Col xs={24} md={12}>
              <Card title="能力评估" bordered={false}>
                <Radar {...radarConfig} height={350} />
              </Card>
            </Col>

            {/* 各模块进度 */}
            <Col xs={24} md={12}>
              <Card title="各模块掌握程度" bordered={false}>
                {progressList.map(item => (
                  <div key={item.type} style={{ marginBottom: 24 }}>
                    <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>
                      <span>{item.name}</span>
                      <span style={{ color: getProgressColor(item.progress), fontWeight: 'bold' }}>
                        {item.progress}%
                      </span>
                    </div>
                    <Progress
                      percent={item.progress}
                      strokeColor={getProgressColor(item.progress)}
                      showInfo={false}
                    />
                  </div>
                ))}
              </Card>
            </Col>

            {/* 学习建议 */}
            <Col xs={24}>
              <Card title="学习建议" bordered={false}>
                {recommendations.length > 0 ? (
                  <List
                    dataSource={recommendations}
                    renderItem={(item) => (
                      <List.Item>
                        <div style={{ display: 'flex', alignItems: 'center', width: '100%' }}>
                          <Tag color={getPriorityColor(item.priority)} style={{ marginRight: 16 }}>
                            优先级：{getPriorityText(item.priority)}
                          </Tag>
                          <span>{item.message}</span>
                        </div>
                      </List.Item>
                                                    )}
                                                  />
                ) : (
                  <Empty description="暂无建议，继续保持！" />
                )}
              </Card>
            </Col>
          </Row>
        ) : (
          <Empty description="暂无分析数据" />
        )}
      </Card>
    </div>
  )
}

export default StudyAnalysis
```

---

## 第 6 页

**文件名：src/pages/Profile.jsx**

```javascript
import React, { useState, useEffect } from 'react'
import { Card, Form, Input, Button, message, Upload, Modal, Descriptions, Avatar } from 'antd'
import { UserOutlined, LockOutlined, UploadOutlined, EditOutlined } from '@ant-design/icons'
import { authService } from '../services/authService'
import { useAuth } from '../context/AuthContext'

const { TextArea } = Input

const Profile = () => {
  const { user, updateProfile } = useAuth()
  const [form] = Form.useForm()
  const [passwordForm] = Form.useForm()
  const [editMode, setEditMode] = useState(false)
  const [passwordModalVisible, setPasswordModalVisible] = useState(false)
  const [loading, setLoading] = useState(false)
  const [avatarUrl, setAvatarUrl] = useState('')

  useEffect(() => {
    if (user) {
      form.setFieldsValue({
        nickname: user.nickname || '',
        email: user.email || '',
        phone: user.phone || '',
        school: user.school || '',
        grade: user.grade || '',
        interests: user.interests || '',
        bio: user.bio || ''
      })
      setAvatarUrl(user.avatar || '')
    }
  }, [user, form])

  const handleEdit = () => {
    setEditMode(true)
  }

  const handleCancel = () => {
    setEditMode(false)
    form.resetFields()
  }

  const handleSubmit = async (values) => {
    setLoading(true)
    try {
      const result = await updateProfile(values)
      if (result.success) {
        message.success('个人信息更新成功')
        setEditMode(false)
      }
    } catch (error) {
      message.error('更新失败，请稍后重试')
    } finally {
      setLoading(false)
    }
  }

  const handlePasswordSubmit = async (values) => {
    setLoading(true)
    try {
      const result = await authService.changePassword(values.oldPassword, values.newPassword)
      if (result.success) {
        message.success('密码修改成功')
        setPasswordModalVisible(false)
        passwordForm.resetFields()
      }
    } catch (error) {
      message.error('密码修改失败')
    } finally {
      setLoading(false)
    }
  }

  const handleAvatarChange = (info) => {
    if (info.file.status === 'done') {
      setAvatarUrl(info.file.response?.url || '')
      message.success('头像上传成功')
    }
  }

  const uploadButton = (
    <div>
      <UploadOutlined />
      <div style={{ marginTop: 8 }}>上传头像</div>
    </div>
  )

  return (
    <div className="profile-container">
      <Card title="个人资料">
        {/* 头像区域 */}
        <div style={{ textAlign: 'center', marginBottom: 32 }}>
          <Avatar
            size={120}
            src={avatarUrl}
            icon={!avatarUrl && <UserOutlined />}
            style={{ marginBottom: 16 }}
          />
          <div>
            <Upload
              name="avatar"
              showUploadList={false}
              action="/api/upload/avatar"
              onChange={handleAvatarChange}
            >
              <Button icon={<UploadOutlined />} style={{ marginTop: 8 }}>
                更换头像
              </Button>
            </Upload>
          </div>
        </div>

        {/* 用户信息 */}
        <Descriptions
          title="基本信息"
          bordered
          column={2}
          style={{ marginBottom: 24 }}
        >
          <Descriptions.Item label="用户名">{user?.username}</Descriptions.Item>
          <Descriptions.Item label="昵称">{user?.nickname || '-'}</Descriptions.Item>
          <Descriptions.Item label="邮箱">{user?.email || '-'}</Descriptions.Item>
          <Descriptions.Item label="手机号">{user?.phone || '-'}</Descriptions.Item>
          <Descriptions.Item label="学校">{user?.school || '-'}</Descriptions.Item>
          <Descriptions.Item label="年级">{user?.grade || '-'}</Descriptions.Item>
        </Descriptions>

        {/* 编辑表单 */}
        <Card
          title="详细资料"
          extra={
            !editMode && (
              <Button icon={<EditOutlined />} onClick={handleEdit}>
                编辑
              </Button>
            )
          }
        >
          <Form
            form={form}
            layout="vertical"
            onFinish={handleSubmit}
          >
            <Form.Item
              label="昵称"
              name="nickname"
              rules={[{ max: 50, message: '昵称不能超过50个字符' }]}
            >
              <Input disabled={!editMode} placeholder="请输入昵称" />
            </Form.Item>

            <Form.Item
              label="邮箱"
              name="email"
              rules={[
                { type: 'email', message: '请输入有效的邮箱地址' }
              ]}
            >
              <Input disabled={!editMode} placeholder="请输入邮箱" />
            </Form.Item>

            <Form.Item
              label="手机号"
              name="phone"
              rules={[
                { pattern: /^1[3-9]\d{9}$/, message: '请输入有效的手机号' }
              ]}
            >
              <Input disabled={!editMode} placeholder="请输入手机号" />
            </Form.Item>

            <Form.Item label="学校" name="school">
              <Input disabled={!editMode} placeholder="请输入学校名称" />
            </Form.Item>

            <Form.Item label="年级" name="grade">
              <Input disabled={!editMode} placeholder="请输入年级" />
            </Form.Item>

            <Form.Item label="兴趣爱好" name="interests">
              <TextArea disabled={!editMode} rows={3} placeholder="请输入兴趣爱好" />
            </Form.Item>

            <Form.Item label="个人简介" name="bio">
              <TextArea disabled={!editMode} rows={4} placeholder="请输入个人简介" />
            </Form.Item>

            {editMode && (
              <Form.Item>
                <Button
                  style={{ marginRight: 16 }}
                  onClick={handleCancel}
                >
                  取消
                </Button>
                <Button
                  type="primary"
                  htmlType="submit"
                  loading={loading}
                >
                  保存
                </Button>
              </Form.Item>
            )}
          </Form>
        </Card>

        {/* 修改密码按钮 */}
        <Card title="安全设置" style={{ marginTop: 16 }}>
          <Button
            type="primary"
            icon={<LockOutlined />}
            onClick={() => setPasswordModalVisible(true)}
          >
            修改密码
          </Button>
        </Card>
      </Card>

      {/* 修改密码弹窗 */}
      <Modal
        title="修改密码"
        open={passwordModalVisible}
        onCancel={() => setPasswordModalVisible(false)}
        footer={null}
      >
        <Form
          form={passwordForm}
          layout="vertical"
          onFinish={handlePasswordSubmit}
        >
          <Form.Item
            label="原密码"
            name="oldPassword"
            rules={[{ required: true, message: '请输入原密码' }]}
          >
            <Input.Password placeholder="请输入原密码" />
          </Form.Item>

          <Form.Item
            label="新密码"
            name="newPassword"
            rules={[
              { required: true, message: '请输入新密码' },
              { min: 6, message: '密码长度不能少于6位' }
            ]}
          >
            <Input.Password placeholder="请输入新密码" />
          </Form.Item>

          <Form.Item
            label="确认密码"
            name="confirmPassword"
            dependencies={['newPassword']}
            rules={[
              { required: true, message: '请确认新密码' },
              ({ getFieldValue }) => ({
                validator(_, value) {
                  if (!value || getFieldValue('newPassword') === value) {
                    return Promise.resolve()
                  }
                  return Promise.reject(new Error('两次输入的密码不一致'))
                }
              })
            ]}
          >
            <Input.Password placeholder="请再次输入新密码" />
          </Form.Item>

          <Form.Item>
            <Button
              type="primary"
              htmlType="submit"
              loading={loading}
              block
            >
              确认修改
            </Button>
          </Form.Item>
        </Form>
      </Modal>
    </div>
  )
}

export default Profile
```

---

## 第 7 页

**文件名：src/pages/Login.jsx**

```javascript
import React, { useState } from 'react'
import { Form, Input, Button, Card, message, Checkbox } from 'antd'
import { UserOutlined, LockOutlined } from '@ant-design/icons'
import { useNavigate } from 'react-router-dom'
import { useAuth } from '../context/AuthContext'

const Login = () => {
  const [loading, setLoading] = useState(false)
  const navigate = useNavigate()
  const { login } = useAuth()

  const onFinish = async (values) => {
    setLoading(true)
    try {
      const result = await login(values.username, values.password)
      if (result.success) {
        message.success('登录成功')
        navigate('/home')
      }
    } catch (error) {
      // 错误已在 AuthContext 中处理
    } finally {
      setLoading(false)
    }
  }

  return (
    <div style={{
      minHeight: '100vh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      background: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)'
    }}>
      <Card
        style={{
          width: 400,
          boxShadow: '0 4px 24px rgba(0, 0, 0, 0.1)'
        }}
      >
        <div style={{ textAlign: 'center', marginBottom: 32 }}>
          <h1 style={{ color: '#1890ff', marginBottom: 8 }}>AI语文助手</h1>
          <p style={{ color: '#666' }}>体育生语文基础智能辅导系统</p>
        </div>

        <Form
          name="login"
          onFinish={onFinish}
          size="large"
        >
          <Form.Item
            name="username"
            rules={[{ required: true, message: '请输入用户名' }]}
          >
            <Input
              prefix={<UserOutlined />}
              placeholder="用户名"
            />
          </Form.Item>

          <Form.Item
            name="password"
            rules={[{ required: true, message: '请输入密码' }]}
          >
            <Input.Password
              prefix={<LockOutlined />}
              placeholder="密码"
            />
          </Form.Item>

          <Form.Item>
            <Form.Item name="remember" valuePropName="checked" noStyle>
              <Checkbox>记住我</Checkbox>
            </Form.Item>
          </Form.Item>

          <Form.Item>
            <Button
              type="primary"
              htmlType="submit"
              loading={loading}
              block
            >
              登录
            </Button>
          </Form.Item>

          <div style={{ textAlign: 'center' }}>
            <span style={{ color: '#666' }}>还没有账号？</span>
            <a
              href="#"
              onClick={(e) => {
                e.preventDefault()
                message.info('注册功能请联系管理员')
              }}
              style={{ marginLeft: 8 }}
            >
              立即注册
            </a>
          </div>
        </Form>

        <div style={{ marginTop: 24, textAlign: 'center', color: '#999', fontSize: 12 }}>
          <p>默认账号：admin</p>
          <p>默认密码：admin123</p>
        </div>
      </Card>
    </div>
  )
}

export default Login
```

---

## 第 8 页

**文件名：src/pages/Literature.jsx**

```javascript
import React, { useState, useEffect } from 'react'
import { Card, Input, Button, List, Tag, Modal, message, Spin, Tabs } from 'antd'
import { SearchOutlined, BookOutlined, PlayCircleOutlined, StarOutlined, StarFilled } from '@ant-design/icons'
import { studyRecordService } from '../services/studyRecordService'
import { questionService } from '../services/questionService'

const { TabPane } = Tabs
const { TextArea } = Input

const Literature = () => {
  const [poems, setPoems] = useState([])
  const [selectedPoem, setSelectedPoem] = useState(null)
  const [searchKeyword, setSearchKeyword] = useState('')
  const [loading, setLoading] = useState(false)
  const [practiceMode, setPracticeMode] = useState(false)
  const [practiceQuestions, setPracticeQuestions] = useState([])
  const [currentQuestionIndex, setCurrentQuestionIndex] = useState(0)
  const [selectedAnswer, setSelectedAnswer] = useState(null)
  const [showResult, setShowResult] = useState(false)
  const [practiceResult, setPracticeResult] = useState(null)
  const [favoritePoems, setFavoritePoems] = useState([])

  useEffect(() => {
    loadPoems()
    loadFavorites()
  }, [])

  const loadPoems = async () => {
    setLoading(true)
    try {
      const questions = await questionService.getQuestionsByType('literature', { count: 50 })
      // 将题目转换为诗词列表格式
      const poemList = questions.map(q => ({
        id: q.id,
        title: q.content.split('\n')[0].substring(0, 20),
        content: q.content,
        question: q.question || q.content,
        options: q.options,
        answer: q.answer,
        explanation: q.explanation,
        year: q.year,
        source: q.source
      }))
      setPoems(poemList)
    } catch (error) {
      message.error('加载古诗词失败')
    } finally {
      setLoading(false)
    }
  }

  const loadFavorites = () => {
    const favorites = localStorage.getItem('favoritePoems')
    if (favorites) {
      setFavoritePoems(JSON.parse(favorites))
    }
  }

  const handleSearch = () => {
    if (!searchKeyword.trim()) {
      loadPoems()
      return
    }

    const filtered = poems.filter(poem =>
      poem.title.includes(searchKeyword) ||
      poem.content.includes(searchKeyword)
    )
    setPoems(filtered)
  }

  const handlePoemSelect = (poem) => {
    setSelectedPoem(poem)
    setShowResult(false)
    setPracticeMode(false)
  }

  const toggleFavorite = (poem) => {
    let favorites = [...favoritePoems]
    const index = favorites.findIndex(p => p.id === poem.id)

    if (index >= 0) {
      favorites.splice(index, 1)
      message.success('已取消收藏')
    } else {
      favorites.push(poem)
      message.success('收藏成功')
    }

    setFavoritePoems(favorites)
    localStorage.setItem('favoritePoems', JSON.stringify(favorites))
  }

  const isFavorite = (poem) => {
    return favoritePoems.some(p => p.id === poem.id)
  }

  const startPractice = async () => {
    setLoading(true)
    try {
      const questions = await questionService.getQuestionsByType('literature', { count: 10 })
      setPracticeQuestions(questions)
      setCurrentQuestionIndex(0)
      setPracticeMode(true)
      setShowResult(false)
      setSelectedAnswer(null)
      setPracticeResult(null)
    } catch (error) {
      message.error('加载练习题失败')
    } finally {
      setLoading(false)
    }
  }

  const handleAnswerSelect = async (answer) => {
    setSelectedAnswer(answer)
    const currentQuestion = practiceQuestions[currentQuestionIndex]
    const isCorrect = answer === currentQuestion.correctAnswer

    // 保存答题记录
    try {
      await studyRecordService.addStudyRecord({
        type: 'literature',
        content: {
          questionId: currentQuestion.id,
          selectedAnswer: answer,
          isCorrect: isCorrect
        },
        duration: 0,
        score: isCorrect ? 10 : 0,
        totalQuestions: 1,
        correctAnswers: isCorrect ? 1 : 0
      })
    } catch (error) {
      console.error('保存答题记录失败:', error)
    }

    if (isCorrect) {
      message.success('回答正确！+10分')
    } else {
      message.error('回答错误')
    }
  }

  const nextQuestion = () => {
    if (currentQuestionIndex < practiceQuestions.length - 1) {
      setCurrentQuestionIndex(prev => prev + 1)
      setSelectedAnswer(null)
    } else {
      finishPractice()
    }
  }

  const finishPractice = () => {
    const correctCount = practiceQuestions.filter((q, i) => {
      // 简化处理，实际应该记录每题的答题情况
      return true
    }).length

    const result = {
      total: practiceQuestions.length,
      correct: correctCount,
      score: correctCount * 10
    }
    setPracticeResult(result)
    setShowResult(true)
    setPracticeMode(false)
  }

  return (
    <div className="literature-container">
      <Card>
        <Tabs defaultActiveKey="1">
          <TabPane tab="诗词浏览" key="1">
            <div style={{ marginBottom: 16 }}>
              <Input.Search
                placeholder="搜索诗词标题或内容"
                value={searchKeyword}
                onChange={(e) => setSearchKeyword(e.target.value)}
                onSearch={handleSearch}
                enterButton={<SearchOutlined />}
              />
            </div>

            {loading ? (
              <div style={{ textAlign: 'center', padding: '50px' }}>
                <Spin />
              </div>
            ) : (
              <List
                grid={{ gutter: 16, xs: 1, sm: 2, md: 3, lg: 4 }}
                dataSource={poems}
                renderItem={poem => (
                  <List.Item>
                    <Card
                      hoverable
                      onClick={() => handlePoemSelect(poem)}
                      title={poem.title}
                      extra={
                        <Button
                          type="text"
                          icon={isFavorite(poem) ? <StarFilled /> : <StarOutlined />}
                          onClick={(e) => {
                            e.stopPropagation()
                            toggleFavorite(poem)
                          }}
                          style={{ color: isFavorite(poem) ? '#ff4d4f' : '' }}
                        />
                      }
                    >
                      <p style={{
                        overflow: 'hidden',
                        textOverflow: 'ellipsis',
                        display: '-webkit-box',
                        WebkitLineClamp: 2,
                        WebkitBoxOrient: 'vertical'
                      }}>
                        {poem.content}
                      </p>
                      {poem.year && <Tag color="blue">{poem.year}</Tag>}
                      {poem.source && <Tag color="green">{poem.source}</Tag>}
                    </Card>
                  </List.Item>
                )}
              />
            )}
          </TabPane>

          <TabPane tab="练习测试" key="2">
            {!practiceMode ? (
              <div style={{ textAlign: 'center', padding: '50px' }}>
                <h3>古诗词练习</h3>
                <p>通过练习巩固古诗词知识</p>
                <Button
                  type="primary"
                  size="large"
                  onClick={startPractice}
                  loading={loading}
                >
                  开始练习
                </Button>
                {practiceResult && (
                  <div style={{ marginTop: 24 }}>
                    <p>上次练习成绩：{practiceResult.correct}/{practiceResult.total} 题</p>
                    <p>得分：{practiceResult.score} 分</p>
                  </div>
                )}
              </div>
            ) : (
              <div>
                {currentQuestionIndex < practiceQuestions.length ? (
                  <div>
                    <h3>第 {currentQuestionIndex + 1}/{practiceQuestions.length} 题</h3>
                    <Card style={{ marginBottom: 16 }}>
                      <p style={{ fontSize: 16, marginBottom: 16 }}>
                        {practiceQuestions[currentQuestionIndex].question}
                      </p>
                      <List
                        dataSource={practiceQuestions[currentQuestionIndex].options}
                        renderItem={(option, index) => (
                          <List.Item
                            style={{
                              cursor: 'pointer',
                              padding: '12px',
                              border: selectedAnswer === index ? '1px solid #1890ff' : '1px solid #f0f0f0',
                              borderRadius: '4px',
                              marginBottom: '8px',
                              background: selectedAnswer === index ? '#e6f7ff' : '#fff'
                            }}
                            onClick={() => handleAnswerSelect(index)}
                          >
                            {String.fromCharCode(65 + index)}. {option}
                          </List.Item>
                        )}
                      />
                    </Card>
                    <Button
                      type="primary"
                      onClick={nextQuestion}
                      disabled={selectedAnswer === null}
                      block
                    >
                      {currentQuestionIndex < practiceQuestions.length - 1 ? '下一题' : '完成练习'}
                    </Button>
                  </div>
                ) : (
                  <div>
                    <h3>练习完成！</h3>
                    <p>答对：{practiceResult?.correct || 0} 题</p>
                    <p>得分：{practiceResult?.score || 0} 分</p>
                    <Button onClick={() => setPracticeMode(false)}>返回</Button>
                  </div>
                )}
              </div>
            )}
          </TabPane>

          <TabPane tab="我的收藏" key="3">
            {favoritePoems.length > 0 ? (
              <List
                dataSource={favoritePoems}
                renderItem={poem => (
                  <List.Item
                    onClick={() => handlePoemSelect(poem)}
                    style={{ cursor: 'pointer' }}
                  >
                    <List.Item.Meta
                      title={poem.title}
                      description={poem.content.substring(0, 100) + '...'}
                    />
                  </List.Item>
                )}
              />
            ) : (
              <div style={{ textAlign: 'center', padding: '50px', color: '#999' }}>
                暂无收藏的诗词
              </div>
            )}
          </TabPane>
        </Tabs>
      </Card>

      {/* 诗词详情弹窗 */}
      {selectedPoem && !practiceMode && (
        <Modal
          title={selectedPoem.title}
          open={!!selectedPoem}
          onCancel={() => setSelectedPoem(null)}
          footer={null}
          width={600}
        >
          <div style={{ lineHeight: '2', fontSize: 16 }}>
            {selectedPoem.content.split('\n').map((line, index) => (
              <p key={index}>{line}</p>
            ))}
          </div>
          {selectedPoem.source && (
            <p style={{ color: '#666', marginTop: 16 }}>出处：{selectedPoem.source}</p>
          )}
          {selectedPoem.explanation && (
            <div style={{ marginTop: 16 }}>
              <h4>解析</h4>
              <p>{selectedPoem.explanation}</p>
            </div>
          )}
        </Modal>
      )}
    </div>
  )
}

export default Literature
```

---

## 第 9 页

**文件名：src/pages/Idiom.jsx**

```javascript
import React, { useState, useEffect } from 'react'
import { Card, Input, Button, List, Tag, Tabs, Empty, message, Spin } from 'antd'
import { SearchOutlined, SoundFilled } from '@ant-design/icons'
import { questionService } from '../services/questionService'
import { studyRecordService } from '../services/studyRecordService'

const { TabPane } = Tabs

const Idiom = () => {
  const [idioms, setIdioms] = useState([])
  const [searchKeyword, setSearchKeyword] = useState('')
  const [loading, setLoading] = useState(false)
  const [selectedIdiom, setSelectedIdiom] = useState(null)
  const [practiceMode, setPracticeMode] = useState(false)
  const [practiceQuestions, setPracticeQuestions] = useState([])
  const [currentQuestionIndex, setCurrentQuestionIndex] = useState(0)
  const [selectedAnswer, setSelectedAnswer] = useState(null)
  const [showFeedback, setShowFeedback] = useState(false)
  const [practiceResult, setPracticeResult] = useState(null)

  useEffect(() => {
    loadIdioms()
  }, [])

  const loadIdioms = async () => {
    setLoading(true)
    try {
      const questions = await questionService.getQuestionsByType('idiom', { count: 100 })
      setIdioms(questions)
    } catch (error) {
      message.error('加载成语失败')
    } finally {
      setLoading(false)
    }
  }

  const handleSearch = () => {
    if (!searchKeyword.trim()) {
      loadIdioms()
      return
    }

    const filtered = idioms.filter(idiom =>
      idiom.content.includes(searchKeyword) ||
      (idiom.explanation && idiom.explanation.includes(searchKeyword))
    )
    setIdioms(filtered)
  }

  const startPractice = async () => {
    setLoading(true)
    try {
      const questions = await questionService.getQuestionsByType('idiom', { count: 20 })
      setPracticeQuestions(questions)
      setCurrentQuestionIndex(0)
      setPracticeMode(true)
      setShowFeedback(false)
      setSelectedAnswer(null)
      setPracticeResult({ correct: 0, total: questions.length })
    } catch (error) {
      message.error('加载练习题失败')
    } finally {
      setLoading(false)
    }
  }

  const selectAnswer = async (index) => {
    if (showFeedback) return

    setSelectedAnswer(index)
    const currentQuestion = practiceQuestions[currentQuestionIndex]
    const isCorrect = index === currentQuestion.correctAnswer

    // 保存答题记录
    await studyRecordService.addStudyRecord({
      type: 'idiom',
      content: {
        questionId: currentQuestion.id,
        selectedAnswer: index,
        isCorrect: isCorrect
      },
      duration: 0,
      score: isCorrect ? 10 : 0,
      totalQuestions: 1,
      correctAnswers: isCorrect ? 1 : 0
    })

    if (isCorrect) {
      setPracticeResult(prev => ({ ...prev, correct: prev.correct + 1 }))
      message.success('回答正确！')
    } else {
      message.error('回答错误')
    }

    setShowFeedback(true)
  }

  const nextQuestion = () => {
    if (currentQuestionIndex < practiceQuestions.length - 1) {
      setCurrentQuestionIndex(prev => prev + 1)
      setShowFeedback(false)
      setSelectedAnswer(null)
    } else {
      setPracticeMode(false)
      setShowFeedback(false)
    }
  }

  return (
    <div className="idiom-container">
      <Card>
        <Tabs defaultActiveKey="1">
          <TabPane tab="成语学习" key="1">
            <div style={{ marginBottom: 16 }}>
              <Input.Search
                placeholder="搜索成语"
                value={searchKeyword}
                onChange={(e) => setSearchKeyword(e.target.value)}
                onSearch={handleSearch}
                enterButton={<SearchOutlined />}
              />
            </div>

            {loading ? (
              <div style={{ textAlign: 'center', padding: '50px' }}>
                <Spin />
              </div>
            ) : (
              <List
                grid={{ gutter: 16, column: 2 }}
                dataSource={idioms}
                renderItem={idiom => (
                  <List.Item>
                    <Card
                      hoverable
                      onClick={() => setSelectedIdiom(idiom)}
                      title={idiom.content}
                    >
                      <p>{idiom.explanation}</p>
                      {idiom.source && <Tag>{idiom.source}</Tag>}
                    </Card>
                  </List.Item>
                )}
              />
            )}
          </TabPane>

          <TabPane tab="成语练习" key="2">
            {!practiceMode ? (
              <div style={{ textAlign: 'center', padding: '50px' }}>
                <h3>成语练习</h3>
                <p>通过练习巩固成语知识</p>
                <Button
                  type="primary"
                  size="large"
                  onClick={startPractice}
                  loading={loading}
                >
                  开始练习
                </Button>
              </div>
            ) : (
              <div>
                <h3>第 {currentQuestionIndex + 1}/{practiceQuestions.length} 题</h3>
                {currentQuestionIndex < practiceQuestions.length && (
                  <Card>
                    <p style={{ fontSize: 16, marginBottom: 16 }}>
                      {practiceQuestions[currentQuestionIndex].question}
                    </p>
                    <List
                      dataSource={practiceQuestions[currentQuestionIndex].options}
                      renderItem={(option, index) => (
                        <List.Item
                          style={{
                            cursor: showFeedback ? 'default' : 'pointer',
                            padding: '12px',
                            border: '1px solid #f0f0f0',
                            borderRadius: '4px',
                            marginBottom: '8px',
                            background: showFeedback
                              ? index === practiceQuestions[currentQuestionIndex].correctAnswer
                                ? '#f6ffed'
                                : selectedAnswer === index
                                  ? '#fff2f0'
                                  : '#fff'
                              : selectedAnswer === index
                                ? '#e6f7ff'
                                : '#fff'
                          }}
                          onClick={() => selectAnswer(index)}
                        >
                          {String.fromCharCode(65 + index)}. {option}
                        </List.Item>
                      )}
                    />
                    {showFeedback && (
                      <div style={{ marginTop: 16 }}>
                        <p>
                          {selectedAnswer === practiceQuestions[currentQuestionIndex].correctAnswer
                            ? '回答正确！'
                            : '回答错误'}
                        </p>
                        {practiceQuestions[currentQuestionIndex].explanation && (
                          <p>解析：{practiceQuestions[currentQuestionIndex].explanation}</p>
                        )}
                      </div>
                    )}
                    <Button
                      type="primary"
                      onClick={nextQuestion}
                      disabled={!showFeedback}
                      style={{ marginTop: 16 }}
                      block
                    >
                      {currentQuestionIndex < practiceQuestions.length - 1 ? '下一题' : '完成练习'}
                    </Button>
                  </Card>
                )}
              </div>
            )}
          </TabPane>
        </Tabs>
      </Card>

      {/* 成语详情弹窗 */}
      {selectedIdiom && (
        <Modal
          title="成语详情"
          open={!!selectedIdiom}
          onCancel={() => setSelectedIdiom(null)}
          footer={null}
        >
          <h2>{selectedIdiom.content}</h2>
          <p><strong>拼音：</strong>{selectedIdiom.pinyin || '暂无'}</p>
          <p><strong>释义：</strong>{selectedIdiom.explanation}</p>
          {selectedIdiom.source && <p><strong>出处：</strong>{selectedIdiom.source}</p>}
        </Modal>
      )}
    </div>
  )
}

export default Idiom
```

---

## 第 10-30 页

**文件名：src/pages/Pinyin.jsx（节选）**

```javascript
import React, { useState } from 'react'
import { Card, Input, Button, List, message } from 'antd'
import { SoundOutlined } from '@ant-design/icons'

const { TextArea } = Input

const Pinyin = () => {
  const [text, setText] = useState('')
  const [pinyinResult, setPinyinResult] = useState([])

  const handleConvert = () => {
    if (!text.trim()) {
      message.warning('请输入要转换的文字')
      return
    }

    // 简单的拼音转换演示
    const result = text.split('').map(char => ({
      char,
      pinyin: getPinyin(char)
    }))

    setPinyinResult(result)
  }

  const getPinyin = (char) => {
    // 这里应该是实际的拼音转换逻辑
    // 简化处理，返回示例拼音
    return 'pīn yīn'
  }

  const handlePlay = (pinyin) => {
    message.info('播放拼音：' + pinyin)
  }

  return (
    <div className="pinyin-container">
      <Card title="拼音学习">
        <TextArea
          value={text}
          onChange={(e) => setText(e.target.value)}
          placeholder="请输入要转换的文字"
          rows={6}
          style={{ marginBottom: 16 }}
        />
        <Button type="primary" onClick={handleConvert}>
          转换为拼音
        </Button>

        {pinyinResult.length > 0 && (
          <div style={{ marginTop: 24 }}>
            <h3>转换结果：</h3>
            <List
              dataSource={pinyinResult}
              renderItem={(item) => (
                <List.Item
                  style={{ cursor: 'pointer' }}
                  onClick={() => handlePlay(item.pinyin)}
                >
                  <span style={{ fontSize: 24, marginRight: 16 }}>{item.char}</span>
                  <span style={{ fontSize: 18, color: '#1890ff' }}>{item.pinyin}</span>
                  <SoundOutlined style={{ marginLeft: 16, color: '#52c41a' }} />
                </List.Item>
              )}
            />
          </div>
        )}
      </Card>
    </div>
  )
}

export default Pinyin
```

**文件名：src/pages/Correction.jsx（节选）**

```javascript
import React, { useState } from 'react'
import { Card, Button, Input, Alert, Spin } from 'antd'
import { CheckCircleOutlined, LoadingOutlined } from '@ant-design/icons'
import { message } from 'antd'

const { TextArea } = Input

const Correction = () => {
  const [inputText, setInputText] = useState('')
  const [corrections, setCorrections] = useState([])
  const [loading, setLoading] = useState(false)

  const handleCorrect = async () => {
    if (!inputText.trim()) {
      message.warning('请输入要检查的文本')
      return
    }

    setLoading(true)
    try {
      // 模拟文本纠错
      const mockCorrections = [
        {
          original: '错别字',
          correction: '错别字',
          position: 0,
          reason: '示例纠错'
        }
      ]
      setCorrections(mockCorrections)
      message.success('检查完成')
    } catch (error) {
      message.error('检查失败')
    } finally {
      setLoading(false)
    }
  }

  return (
    <div className="correction-container">
      <Card title="文本纠错">
        <TextArea
          value={inputText}
          onChange={(e) => setInputText(e.target.value)}
          placeholder="请输入需要检查的文本"
          rows={10}
          style={{ marginBottom: 16 }}
        />
        <Button
          type="primary"
          onClick={handleCorrect}
          loading={loading}
          icon={loading ? <LoadingOutlined /> : <CheckCircleOutlined />}
        >
          开始检查
        </Button>

        {corrections.length > 0 && (
          <div style={{ marginTop: 24 }}>
            <h3>纠错结果：</h3>
            {corrections.map((item, index) => (
              <Alert
                key={index}
                message={`建议将"${item.original}"修改为"${item.correction}"`}
                description={item.reason}
                type="warning"
                showIcon
                style={{ marginBottom: 8 }}
              />
            ))}
          </div>
        )}
      </Card>
    </div>
  )
}

export default Correction
```

**文件名：src/pages/MockExam.jsx（节选）**

```javascript
import React, { useState } from 'react'
import { Card, Radio, InputNumber, Button, Modal, Result, Progress } from 'antd'
import { ClockCircleOutlined } from '@ant-design/icons'
import { questionService } from '../services/questionService'
import { studyRecordService } from '../services/studyRecordService'

const MockExam = () => {
  const [config, setConfig] = useState({
    duration: 60,
    questionCount: 20,
    types: ['vocabulary', 'literature', 'idiom', 'pinyin']
  })
  const [examStarted, setExamStarted] = useState(false)
  const [questions, setQuestions] = useState([])
  const [currentIndex, setCurrentIndex] = useState(0)
  const [answers, setAnswers] = useState([])
  const [timeLeft, setTimeLeft] = useState(0)
  const [showResult, setShowResult] = useState(false)
  const [examResult, setExamResult] = useState(null)

  const handleStartExam = async () => {
    const allQuestions = []
    for (const type of config.types) {
      const typeQuestions = await questionService.getQuestionsByType(type, {
        count: Math.ceil(config.questionCount / config.types.length)
      })
      allQuestions.push(...typeQuestions)
    }

    setQuestions(allQuestions.slice(0, config.questionCount))
    setAnswers(new Array(config.questionCount).fill(null))
    setTimeLeft(config.duration * 60)
    setExamStarted(true)

    // 倒计时
    const timer = setInterval(() => {
      setTimeLeft(prev => {
        if (prev <= 1) {
          clearInterval(timer)
          handleSubmitExam()
          return 0
        }
        return prev - 1
      })
    }, 1000)
  }

  const handleSelectAnswer = (answer) => {
    const newAnswers = [...answers]
    newAnswers[currentIndex] = answer
    setAnswers(newAnswers)
  }

  const handleSubmitExam = () => {
    let correctCount = 0
    questions.forEach((q, index) => {
      if (answers[index] === q.correctAnswer) {
        correctCount++
      }
    })

    const score = Math.round((correctCount / questions.length) * 100)
    setExamResult({
      total: questions.length,
      correct: correctCount,
      score,
      duration: config.duration * 60 - timeLeft
    })
    setShowResult(true)
    setExamStarted(false)

    // 保存考试记录
    studyRecordService.addStudyRecord({
      type: 'mock_exam',
      content: {
        title: `模拟考试 - ${config.duration}分钟`,
        questionCount: questions.length
      },
      duration: config.duration * 60 - timeLeft,
      score,
      totalQuestions: questions.length,
      correctAnswers: correctCount
    })
  }

  return (
    <div className="mock-exam-container">
      <Card title="模拟考试">
        {!examStarted && !showResult && (
          <div>
            <div style={{ marginBottom: 16 }}>
              <span>考试时长：</span>
              <Radio.Group
                value={config.duration}
                onChange={(e) => setConfig({ ...config, duration: e.target.value })}
                style={{ marginLeft: 16 }}
              >
                <Radio.Button value={30}>30分钟</Radio.Button>
                <Radio.Button value={60}>60分钟</Radio.Button>
                <Radio.Button value={90}>90分钟</Radio.Button>
                <Radio.Button value={120}>120分钟</Radio.Button>
              </Radio.Group>
            </div>

            <div style={{ marginBottom: 16 }}>
              <span>题目数量：</span>
              <InputNumber
                min={10}
                max={100}
                step={10}
                value={config.questionCount}
                onChange={(value) => setConfig({ ...config, questionCount: value })}
                style={{ marginLeft: 16 }}
              />
            </div>

            <Button type="primary" onClick={handleStartExam} size="large">
              开始考试
            </Button>
          </div>
        )}

        {examStarted && (
          <div>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 16 }}>
              <span>第 {currentIndex + 1}/{questions.length} 题</span>
              <span style={{ color: timeLeft < 300 ? 'red' : 'inherit' }}>
                <ClockCircleOutlined /> {Math.floor(timeLeft / 60)}:{(timeLeft % 60).toString().padStart(2, '0')}
              </span>
            </div>

            <Card style={{ marginBottom: 16 }}>
              <p style={{ fontSize: 16 }}>{questions[currentIndex]?.question}</p>
              <Radio.Group
                value={answers[currentIndex]}
                onChange={(e) => handleSelectAnswer(e.target.value)}
                style={{ width: '100%' }}
              >
                {questions[currentIndex]?.options.map((option, index) => (
                  <Radio
                    key={index}
                    value={index}
                    style={{ display: 'block', margin: '8px 0' }}
                  >
                    {String.fromCharCode(65 + index)}. {option}
                  </Radio>
                ))}
              </Radio.Group>
            </Card>

            <div style={{ display: 'flex', justifyContent: 'space-between' }}>
              <Button
                disabled={currentIndex === 0}
                onClick={() => setCurrentIndex(prev => prev - 1)}
              >
                上一题
              </Button>
              <Button
                type="primary"
                onClick={() => {
                  if (currentIndex < questions.length - 1) {
                    setCurrentIndex(prev => prev + 1)
                  } else {
                    handleSubmitExam()
                  }
                }}
              >
                {currentIndex < questions.length - 1 ? '下一题' : '提交试卷'}
              </Button>
            </div>
          </div>
        )}

        {showResult && examResult && (
          <Result
            status={examResult.score >= 60 ? 'success' : 'warning'}
            title={examResult.score >= 60 ? '恭喜通过！' : '继续努力！'}
            subTitle={`得分：${examResult.score}分 | 正确率：${Math.round((examResult.correct / examResult.total) * 100)}%`}
            extra={[
              <Button type="primary" key="again" onClick={() => {
                setShowResult(false)
                setCurrentIndex(0)
                setAnswers([])
              }}>
                再考一次
              </Button>
            ]}
          />
        )}
      </Card>
    </div>
  )
}

export default MockExam
```

**文件名：src/services/questionService.js（节选）**

```javascript
import api from './api';

class QuestionService {
  async getQuestions(params) {
    try {
      const response = await api.get('/questions', { params });
      if (response.success) {
        return response.data;
      }
      return [];
    } catch (error) {
      console.error('获取题目失败:', error);
      return [];
    }
  }

  async getQuestionsByType(type, options = {}) {
    try {
      const params = {
        type,
        limit: options.count || 10,
        year: options.year,
        difficulty: options.difficulty
      };
      const response = await api.get('/questions/type/' + type, { params });
      if (response.success) {
        return response.data;
      }
      return [];
    } catch (error) {
      console.error('获取题目失败:', error);
      return [];
    }
  }

  async getRandomQuestions(options = {}) {
    try {
      const params = {
        limit: options.count || 10,
        type: options.type,
        difficulty: options.difficulty,
        year: options.year
      };
      const response = await api.get('/questions/random', { params });
      if (response.success) {
        return response.data;
      }
      return [];
    } catch (error) {
      console.error('获取随机题目失败:', error);
      return [];
    }
  }

  async getQuestionById(id) {
    try {
      const response = await api.get('/questions/' + id);
      if (response.success) {
        return response.data;
      }
      return null;
    } catch (error) {
      console.error('获取题目详情失败:', error);
      return null;
    }
  }
}

const questionService = new QuestionService();
export { questionService };
```

**文件名：src/index.css（节选）**

```css
body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
    'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
    sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

code {
  font-family: source-code-pro, Menlo, Monaco, Consolas, 'Courier New',
    monospace;
}

#root {
  --primary-color: #1890ff;
  --success-color: #52c41a;
  --warning-color: #faad14;
  --error-color: #f5222d;
  --text-color: #000000d9;
  --border-color: #d9d9d9;
}

* {
  box-sizing: border-box;
}
```

**文件名：src/App.css（节选）**

```css
.layout {
  min-height: 100vh;
}

.layout .header {
  display: flex;
  align-items: center;
  position: fixed;
  z-index: 1;
  width: 100%;
  background: #001529;
}

.layout .header .logo {
  float: left;
  color: #fff;
  font-size: 20px;
  font-weight: bold;
  padding: 0 24px;
  height: 64px;
  line-height: 64px;
}

.layout .header .header-menu {
  flex: 1;
}

.layout .content {
  padding: 88px 50px 24px;
  min-height: 100vh;
  background: #f0f2f5;
}

.layout .footer {
  text-align: center;
  background: #001529;
  color: rgba(255, 255, 255, 0.65);
  padding: 24px 50px;
}

@media (max-width: 768px) {
  .layout .content {
    padding: 72px 16px 24px;
  }
}
```

---

**（后30页结束，共1500行源代码）**

---

## 页码说明

本源代码鉴别材料包含：
- 前30页：第1-1500行源代码
- 后30页：第1451-2950行源代码（与最后1500行对应）

每页约50行代码，符合软著申请要求。

---

**文件生成日期：2025年01月27日**
